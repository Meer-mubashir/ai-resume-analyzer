experimentalServices:
  frontend:
    root: frontend
    routePrefix: /
    framework: vite
  backend:
    root: backend
    routePrefix: /_/backend
    docker:
      cmd: npx prisma db push && node server.js
